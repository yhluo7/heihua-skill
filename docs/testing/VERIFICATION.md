# v0.1.0 验证记录

验证日期，2026年9月12日。

## 已实际运行

环境为 Linux，Python 3.13.5。辅助工具本身只依赖标准库。

| 检查 | 实际结果 | 不代表什么 |
| --- | --- | --- |
| unittest 工具测试 | 51项通过，0失败，0错误 | 不代表模型意图判断准确率 |
| 八个参考工作包 | 结构、证据引用、逐字引文及已声明门槛校验通过 | 不证明假设就是上级真实意图 |
| 模拟 CSV 算术复算 | 线索增长44.0%，成交增长8.0%，转化率10.0%到7.5%，减少2.5个百分点 | 不代表真实业务效果或因果关系 |
| 独立 JSON Schema 检查 | 使用环境内 jsonschema 的 Draft 2020-12 校验器，8个参考 JSON 均通过 | 仓库的轻量校验器不是通用标准实现 |
| YAML 解析 | SKILL frontmatter、界面元数据及 CI 配置可解析 | 不代表 GitHub Actions 或客户端已运行成功 |
| 浏览器内容渲染 | Chromium 实际渲染 HTML，桌面1500像素和移动420像素无横向页面溢出，折叠面板可展开 | 不代表所有浏览器兼容性均已实测 |
| 本地安装测试 | 临时目录内预览不写入，安装只复制运行资源，拒绝覆盖已有目录，安装后工具可读资源 | 不代表用户 Windows 或 Claude/Codex 客户端已加载成功 |

页面通过浏览器注入本地生成的 HTML 内容完成渲染和截图。此环境直接 file URL 导航受到限制，因此没有把此环境的文件双击打开写成实测结果。预览图已查看，确认中文显示和主要内容未被遮挡。

[完整 unittest 日志](unittest.txt) 和 [Demo 算术与结构日志](demo-check.txt) 随包提供。工具测试先写断言再实现，初始失败指向缺少的实现模块。后续修正过一处测试数据自身的编号问题，没有用修改测试来放宽授权约束。

## 静态仓库检查

`python tools/check_repo.py` 检查主入口、Python 3.10语法兼容性、内部文件链接、60条词库、8套输入哈希与参考输出、16个待评估场景、7个竞品来源，以及离线页面和当前参考结果是否一致。

该工具不联网，所以不检查外部链接今天是否可达。来源调研记录与静态检查是两项不同工作。语法兼容不等于已在 Python 3.10 解释器运行。

## 未运行的部分

没有真实用户测试，没有无 Skill 对照实验，没有与竞品的端到端效果比较，没有在用户 Windows 客户端或 Claude Code 客户端实测。未执行 GitHub Actions 矩阵，也没有测试模型在所有压力场景下的行为稳定性。

`examples` 中8个输出是制作本项目时编写的合成参考结果，不是独立宿主模型的调用日志。`eval` 中16个场景尚未运行。不能从本地脚本51项测试通过，推导出模型会正确解释51种老板意图。

代码与案例进行了当前制作会话内的检查，没有冒称经过独立评审者或子代理审查。

## 复现命令

在仓库根目录运行。

```bash
python -m unittest discover -s tests -v
python tools/check_repo.py
python tools/run_demo.py --check
```

真实模型评估请按 [评估协议](../../eval/README.md) 进行，特别是不要将参考输出提供给被测模型。
